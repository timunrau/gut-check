# Future Steps

After the first manual run:

1. Add focused backend tests for classification, time defaulting, parser failure save behavior, and follow-up limits.
2. Write a concise README with Docker Compose startup, Ollama model pull, LAN/Tailscale access, env vars, and privacy notes.
3. Add backup/reset notes for `./data/gutcheck.db`.
4. Decide whether event editing is needed or delete/re-log is enough for the MVP.

